# Dw3000

This is the Dw3000 library developed by NConcepts, simply consumed as a dependency by our Esp32 firmware.

We slightly patched this library, fixing macro redefinition warnings and const-correctness of char pointers.
Then we applied clang-format to all files in the library.
